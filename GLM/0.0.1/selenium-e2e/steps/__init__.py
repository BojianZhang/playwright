# steps/ —— z.ai 流程阶段包(被入口以 `from steps import xxx` 导入)
#   steps_auth      注册(滑块验证 + 邮箱链接验证 + 完成注册) / 登录(滑块验证)
#   steps_apikey    创建并抓取 API Key(z.ai/manage-apikey)
#   steps_subscribe 选套餐(Lite/Pro/Max × Monthly/Quarterly/Yearly) + 信用卡支付
